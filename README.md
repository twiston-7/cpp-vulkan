# Building
## Dependencies
Vulkan SDK
glfw3

## How to build
In the project root, run:
```sh
mkdir build
cd build 
cmake .. 
make
```

# Running
After building, the output should be a file named `main` or `main.exe`. Simply execute this file to run the project! 